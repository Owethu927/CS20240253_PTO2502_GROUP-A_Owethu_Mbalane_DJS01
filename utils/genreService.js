import { genres } from "../data";

/**
 * @param {object}
 * @param {genres}
 */

export function genreServices() {
  let genre = Object.entries(genres).map(([key, values]) => {
    return genre;
  });
}
